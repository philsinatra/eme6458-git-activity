1. Type 'npm install' in the terminal while in the main directory (the 'beta' folder)
2. Type 'gulp' in the terminal, which will run the process
3. Files will be placed in the newly created 'dist' folder
